<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="NVLite.App.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    NavigationCacheMode="Enabled">

    <ScrollViewer Padding="24">
        <StackPanel Spacing="16" MaxWidth="600" HorizontalAlignment="Left">
            <TextBlock Text="Settings" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,8" />

            <!-- Theme -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="20">
                <StackPanel Spacing="12">
                    <TextBlock Text="Appearance" Style="{StaticResource SubtitleTextBlockStyle}" />
                    <TextBlock Text="Theme" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    <RadioButtons SelectedIndex="{x:Bind ViewModel.SelectedThemeIndex, Mode=TwoWay}">
                        <RadioButton Content="System default" />
                        <RadioButton Content="Light" />
                        <RadioButton Content="Dark" />
                    </RadioButtons>
                </StackPanel>
            </Border>

            <!-- System -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="20">
                <StackPanel Spacing="12">
                    <TextBlock Text="System" Style="{StaticResource SubtitleTextBlockStyle}" />
                    <ToggleSwitch Header="Minimize to system tray on close"
                                  IsOn="{x:Bind ViewModel.MinimizeToTray, Mode=TwoWay}" />
                    <ToggleSwitch Header="Start with Windows"
                                  IsOn="{x:Bind ViewModel.StartWithWindows, Mode=TwoWay}" />
                </StackPanel>
            </Border>

            <!-- Monitoring -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="20">
                <StackPanel Spacing="12">
                    <TextBlock Text="Monitoring" Style="{StaticResource SubtitleTextBlockStyle}" />
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <TextBlock Text="Polling interval (seconds)"
                                   VerticalAlignment="Center"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <NumberBox Value="{x:Bind ViewModel.PollingIntervalSeconds, Mode=TwoWay}"
                                   Minimum="1" Maximum="10"
                                   SpinButtonPlacementMode="Inline"
                                   SmallChange="1" LargeChange="5"
                                   Width="120" />
                    </StackPanel>
                    <ToggleSwitch Header="GPU temperature alerts"
                                  IsOn="{x:Bind ViewModel.EnableTempAlerts, Mode=TwoWay}" />
                    <ToggleSwitch Header="Show temperatures in Fahrenheit"
                                  IsOn="{x:Bind ViewModel.UseFahrenheit, Mode=TwoWay}" />
                    <StackPanel Orientation="Horizontal" Spacing="12"
                                Visibility="{x:Bind ViewModel.EnableTempAlerts, Mode=OneWay, Converter={StaticResource BoolToVisibility}}">
                        <TextBlock Text="Alert threshold (°C)"
                                   VerticalAlignment="Center"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <NumberBox Value="{x:Bind ViewModel.GpuTempAlertThreshold, Mode=TwoWay}"
                                   Minimum="50" Maximum="110"
                                   SpinButtonPlacementMode="Inline"
                                   SmallChange="5" LargeChange="10"
                                   Width="120" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Drivers -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="20">
                <StackPanel Spacing="12">
                    <TextBlock Text="Drivers" Style="{StaticResource SubtitleTextBlockStyle}" />
                    <ToggleSwitch Header="Check for driver updates on startup"
                                  IsOn="{x:Bind ViewModel.CheckDriverOnStartup, Mode=TwoWay}" />
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <TextBlock Text="Auto-check interval (hours, 0 = off)"
                                   VerticalAlignment="Center"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <NumberBox Value="{x:Bind ViewModel.DriverCheckIntervalHours, Mode=TwoWay}"
                                   Minimum="0" Maximum="168"
                                   SpinButtonPlacementMode="Inline"
                                   SmallChange="1" LargeChange="12"
                                   Width="120" />
                    </StackPanel>
                    <TextBlock Text="Set to 0 to disable. Requires app restart to take effect."
                               Style="{StaticResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
